{
  "name": "app",
  "private": true,
  "scripts": {
    "build": "esbuild app/javascript/**/*.js --bundle --sourcemap --format=esm --outdir=app/assets/builds/js --public-path=/assets/js",
    "build:watch": "npm run build -- --watch"
  },
  "dependencies": {
    "jquery": "^3.7.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  }
}
